box: node:6.9-slim
dev:
  steps:
    - npm-install
    - internal/watch:
        code: grunt serve
        reload: true

build:
  steps:
    - script:
        code: export NODE_ENV='testing'
    - npm-install
    - hgen/gulp:
        tasks: test
